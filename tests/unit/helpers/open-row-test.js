import { openRow } from '../../../helpers/open-row';
import { module, test } from 'qunit';

module('Unit | Helper | open row');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = openRow(42);
  assert.ok(result);
});
